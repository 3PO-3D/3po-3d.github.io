source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-feed"
gem "jekyll-sitemap"

# Ruby 3.4+ removed these from the default gems; needed for local jekyll builds.
# Harmless on GitHub Pages (which builds in its own pinned environment).
gem "csv"
gem "base64"
gem "bigdecimal"
